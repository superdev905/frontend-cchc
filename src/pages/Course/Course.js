import { Box } from '@material-ui/core'
import { PageHeading, Wrapper } from '../../components/UI'

const Course = () => (
  <Wrapper>
    <Box display="flex" justifyContent="space-between">
      <PageHeading>Curso Ejemplo</PageHeading>
    </Box>
  </Wrapper>
)

export default Course
